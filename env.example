# =================================
# Fast Data Agent 环境配置示例
# =================================
# 复制此文件为 .env 并根据需要修改配置

# 环境配置 (development | production)
# development: 开发环境 - 启用热重载、DEBUG日志、详细错误信息
# production:  生产环境 - 禁用热重载、强制安全密钥、隐藏错误详情
ENVIRONMENT=development

# 数据库配置（仅支持 PostgreSQL）
DATABASE_URL=postgresql+asyncpg://postgres:postgres123@localhost:5432/data_agent

# JWT 配置（生产环境务必修改）
# 可使用以下命令生成随机密钥: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your-secret-key-change-in-production
REFRESH_SECRET_KEY=your-refresh-secret-key-change-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# 应用配置
APP_NAME=fast-data-agent
DEBUG=true

# 日志配置
# 开发环境默认 DEBUG，生产环境默认 INFO
# 可选值: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# API 文档配置
# 开发环境始终启用，生产环境可通过此配置禁用
ENABLE_DOCS=true

# CORS 配置（多个来源用逗号分隔）
# 开发环境
ALLOWED_ORIGINS=["*"]
# 生产环境示例
# ALLOWED_ORIGINS=["https://yourdomain.com", "https://api.yourdomain.com"]

# 服务器配置
HOST=0.0.0.0
PORT=8000
